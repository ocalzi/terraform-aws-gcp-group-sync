# AWS Credentials
# Recommended: use AWS credentials file (~/.aws/credentials),
# environment variables (AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY),
# or IAM roles instead of hardcoding keys here.
# access_key = "YOUR_AWS_ACCESS_KEY"
# secret_key = "YOUR_AWS_SECRET_KEY"

# AWS Configuration
aws_region = "eu-west-3"

# Group Mappings
# Map Google Workspace groups to AWS Identity Center groups.
# aws_policy_arn is optional — reserved for future permission set support.
group_mappings = {
  example_admin = {
    gcp_group_email = "admins@example.com"
    aws_role_name   = "GCP-Admin-Role"
    aws_policy_arn  = "arn:aws:iam::aws:policy/AdministratorAccess"
  }
  example_readonly = {
    gcp_group_email = "viewers@example.com"
    aws_role_name   = "GCP-ReadOnly-Role"
    # aws_policy_arn is optional, can be omitted
  }
  example_developers = {
    gcp_group_email = "developers@example.com"
    aws_role_name   = "GCP-Developer-Role"
    aws_policy_arn  = "arn:aws:iam::aws:policy/PowerUserAccess"
  }
}
